<%= simple_form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= "#{pluralize(@post.errors.count, "error")} prohibited this post from being saved:" %>
      </h2>
      <ul>
        <% @post.errors.full_messages.each do |msg| %>
          <li>
            <%= msg %>
          </li>
          <% end %>
      </ul>
    </div>
  <% end %>



  <div class="heading" style="text-align:center"  ;"><b>What's new</b></div>
<div class="blogcontainer">
  <div class="form-group">

  <div class="form-group">  <%= f.input :username ,  class: "form-control"%> </div>
   <div class="form-group">    <%= f.input :description, class: "form-control" %> </div>
  </div>
  <div class="btn btn-primary"> <%= f.button :submit %> </div>
  </div>

<% end %>
